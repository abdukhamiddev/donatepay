import Button from "@mui/material/Button";
import { styled } from "@mui/system";

const CmButton = styled(Button)({
	padding: "1rem 2rem",
	fontSize: "1.2rem",
	fontFamily: "Montserrat",
	borderRadius: "10px",
	fontWeight: 500,
	textTransform: "none",
});

export default CmButton;
